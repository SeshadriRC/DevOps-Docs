# Kubernetes OOMKill Loops

## Incident Details

| Field | Value |
|-------|-------|
| **Severity** | SEV-3 (Moderate) |
| **Service** | User Auth Service |
| **Duration** | Intermittent (2 Weeks) |
| **Status** | Resolved |
| **Owner** | DevOps Team |

---

## 1. Issue Summary

The Authentication Service pods were restarting randomly 10-15 times per day. Users reported intermittent session drops ("I keep getting logged out"). Kubernetes status showed OOMKilled (Out Of Memory).

---

## 2. Business Impact

### User Impact
Degraded User Experience (UX). Frustration due to forced re-logins.

### System Impact
Increased latency during pod startup times (Cold starts).

---

## 3. Root Cause Analysis

**Why were pods crashing?**  
The container memory usage exceeded the Kubernetes limits.

**Why did it exceed the limit?**

### Memory Configuration

| Component | Configured Value |
|-----------|------------------|
| **K8s Limit** | 1Gi (1024 MB) |
| **Java Heap (Xmx)** | 768 MB |

### The Miscalculation
The engineering team assumed 768MB fits inside 1024MB. However, they failed to account for JVM Overhead (Metaspace, Thread Stacks, Garbage Collection, Native Code).

### Actual Memory Usage
- Heap: 768MB
- Non-Heap: ~300MB
- **Total: 1068MB** (exceeds 1024MB limit)

### Root Cause
**Configuration Error.** Misunderstanding of Java memory architecture versus Container Hard Limits.

---

## 4. Resolution & Fix

### Immediate Fix
Increased Kubernetes Memory Limit to 1.5Gi to provide immediate headroom.

### Permanent Fix
- Tuned JVM parameters: Reduced Heap (`-Xmx`) to 512m (approx 50-60% of container limit)
- Enabled JVM flag `-XX:+ExitOnOutOfMemoryError` to allow clean restarts instead of hangs

---

## 5. Action Items

- **[High]** Create a "Golden Signals" dashboard for Memory Usage vs. Limit %
- **[Medium]** Conduct a "Container Sizing" workshop for the backend team

---
# Chatgpt summarize


This is a very common Kubernetes + Java issue. The confusing part is that many people think:

```text
Container Limit = 1 GB
Java Heap = 768 MB

768 < 1024

So everything should work.
```

But that's not how Java memory works.

---

# Step 1: What was happening?

Users reported:

```text
Login works
↓
After some time
↓
Logged out unexpectedly
```

DevOps team checks pods:

```bash
kubectl get pods
```

Output:

```text
auth-service-abc123   Running
auth-service-def456   Running
```

A few minutes later:

```text
auth-service-abc123   Running
auth-service-def456   Running
```

Looks fine.

But:

```bash
kubectl get pods
```

again:

```text
auth-service-abc123   Running   RESTARTS=12
auth-service-def456   Running   RESTARTS=15
```

Pods are restarting continuously.

---

# Step 2: Check Why Pod Restarted

Run:

```bash
kubectl describe pod auth-service-abc123
```

Output:

```text
Last State:
  Terminated
  Reason: OOMKilled
```

This is the clue.

---

# What is OOMKilled?

OOM = Out Of Memory

Kubernetes says:

> "Container crossed the memory limit. I killed it."

---

# Step 3: Check Deployment Configuration

Suppose deployment has:

```yaml
resources:
  requests:
    memory: "512Mi"

  limits:
    memory: "1Gi"
```

Meaning:

```text
Container can use
up to 1024 MB
```

If it crosses:

```text
1025 MB
```

Kubernetes kills it immediately.

---

# Step 4: What Developers Configured

Java startup command:

```bash
java -Xmx768m -jar auth.jar
```

Developers think:

```text
Container Limit = 1024 MB
Heap = 768 MB

Remaining = 256 MB

Looks safe.
```

But they forgot something.

---

# Java Uses More Than Heap

Many people think:

```text
Java Memory = Heap
```

Wrong.

Java process consists of:

```text
Heap
+
Metaspace
+
Thread Stacks
+
Garbage Collector
+
Native Libraries
+
JIT Compiler
```

---

# Real Memory Usage

Heap:

```text
768 MB
```

Metaspace:

```text
100 MB
```

Thread Stacks:

```text
80 MB
```

GC Structures:

```text
40 MB
```

Native Memory:

```text
80 MB
```

Total:

```text
768
+100
+80
+40
+80
------
1068 MB
```

---

# Container Limit

Kubernetes limit:

```text
1024 MB
```

Actual usage:

```text
1068 MB
```

Result:

```text
1068 > 1024
```

Kubernetes kills container.

---

# Visual Representation

Container:

```text
+----------------------+
| Limit = 1024 MB      |
+----------------------+

Heap         768 MB
Metaspace    100 MB
Threads       80 MB
GC            40 MB
Native        80 MB
----------------------
Total       1068 MB
```

Exceeded.

OOMKilled.

---

# Why Users Were Logged Out

Suppose requests are reaching:

```text
Pod-1
Pod-2
Pod-3
```

User logs in.

Session stored in memory.

Then:

```text
Pod-2 OOMKilled
```

Pod dies.

Memory disappears.

User session disappears.

User sees:

```text
Please login again
```

---

# How DevOps Team Verified

Check current memory usage:

```bash
kubectl top pod
```

Example:

```text
NAME              MEMORY
auth-service-1     980Mi
auth-service-2    1015Mi
auth-service-3    1008Mi
```

Very close to:

```text
1024 Mi
```

Huge clue.

---

# Immediate Fix

Increase memory limit.

Before:

```yaml
limits:
  memory: "1Gi"
```

After:

```yaml
limits:
  memory: "1500Mi"
```

or

```yaml
limits:
  memory: "1.5Gi"
```

Now:

```text
Actual Usage = 1068 MB
Limit = 1536 MB
```

Safe.

---

# Why This Is Only a Temporary Fix

Giving more RAM is like:

```text
Painkiller
```

It reduces the symptom.

But doesn't fix the sizing problem.

---

# Permanent Fix

Reduce heap size.

Before:

```bash
-Xmx768m
```

After:

```bash
-Xmx512m
```

Now memory usage becomes:

```text
Heap         512 MB
Metaspace    100 MB
Threads       80 MB
GC            40 MB
Native        80 MB
----------------------
Total        812 MB
```

Container limit:

```text
1024 MB
```

Safe.

---

# Why 50-60% Rule?

A common Java container guideline:

```text
Heap should be
50% - 60%
of container memory
```

Example:

```text
Container = 1 GB
```

Heap:

```text
512 MB
or
600 MB
```

Leave space for:

```text
Metaspace
Threads
Native Memory
GC
```

---

# What is This Flag?

```bash
-XX:+ExitOnOutOfMemoryError
```

Without it:

```text
Java may hang
Not respond
Not exit cleanly
```

With it:

```text
OOM occurs
↓
JVM exits immediately
↓
Kubernetes restarts pod
```

Recovery is faster.

---

# How Monitoring Would Catch It

Prometheus metrics:

```text
container_memory_usage_bytes
container_spec_memory_limit_bytes
```

Grafana graph:

```text
Memory Usage %

70%
80%
90%
95%
99%
100%
OOMKilled
```

Alert:

```text
Memory Usage > 80%
for 5 minutes
```

Team gets notified before users are impacted.

---

## Interview Answer

> The Authentication Service pods were being OOMKilled because the Kubernetes memory limit was configured as 1Gi, while the JVM heap was set to 768MB. The team assumed 768MB would fit comfortably inside a 1Gi container, but they overlooked JVM overhead such as Metaspace, thread stacks, garbage collection structures, and native memory. The actual process consumed around 1068MB, exceeding the container limit. Kubernetes therefore terminated the container with an OOMKilled event. We confirmed this using `kubectl describe pod` and `kubectl top pod`, increased the memory limit as an immediate mitigation, and later reduced the JVM heap size to 512MB to keep total memory usage safely within container limits.

